fn main() -> u8 {
  $entry:
    var_ptr = cpos 0_u8
    var = load var_ptr
    switch var, $default {
        0_u8 => $case_zero
        1_u8 => $case_one
    }
  $case_zero:
    res = call increment(var)
    ret res
  $case_one:
    cbr var, $case_zero, $default
  $default:
    br $case_zero
}

fn increment(value: u8) -> u8 {
  $entry:
    one_ptr = cpos 1_u8
    one = load one_ptr
    res = add value, one
    ret res
}
